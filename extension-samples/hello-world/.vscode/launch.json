{
    "configurations": [
        {
            "name": "Launch Extension",
            "type": "extensionHost",
            "request": "launch",
            "args": [
                "--extensionDevelopmentPath=${workspaceFolder}/out",
                //
                "--disable-extensions"
            ],
            "outFiles": ["${workspaceFolder}/out/**/*.js"]
        }
    ]
}
