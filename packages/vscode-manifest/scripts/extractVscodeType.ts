// The first step. Extract TS type (and patch) from VSCode

// TODO finish implementation

const main = async () => {
    // const vscodeSourceFile =
    // Too dirty . Try to adjust the ast?
}

main().catch(error => {
    console.error(error)
    process.exit(1)
})
